# Run with `flutter pub run ffigen --config ffigen.yaml`.
name: SphereUniformGeocodingBindings
description: |
  Bindings for `src/sphere_uniform_geocoding.h`.

  Regenerate bindings with `flutter pub run ffigen --config ffigen.yaml`.
output: 'lib/sphere_uniform_geocoding_bindings_generated.dart'
headers:
  entry-points:
    - 'src/sphere_uniform_geocoding.h'
  include-directives:
    - 'src/sphere_uniform_geocoding.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
